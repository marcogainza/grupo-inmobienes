export default function SectionEyebrow({ text }: { text: string }) {
  return (
    <span className="inline-block text-xs font-semibold uppercase tracking-widest text-blue-accent">
      {text}
    </span>
  );
}
